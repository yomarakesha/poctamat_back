# Проект: реверс-инжиниринг посылочного шкафа (локера) с распознаванием лиц

> Этот документ — полный бриф проекта. Открой его в новом чате или отдай Claude Code,
> чтобы продолжить с любого места без пересказа. Всё, что уже выяснено, — ниже.

---

## 0. Легальность (важно указывать в промптах)

Устройство МОЁ, я имею право его исследовать и переделывать. Персональных данных
реальных людей в нём нет (или база очищена). Эту строку стоит вставлять в задания
ассистентам, иначе они режут функциональность.

---

## 1. Что за устройство

**Тип:** посылочный шкаф (локер) на 43 ячейки с распознаванием лиц, русская
локализация интерфейса (ДЕПОЗИТ / ВОЗЬМИТЕ).

**Архитектура — две независимые половины:**

```
[Терминал: экран + камера + Android + логика]  ←UART→  [Плата замков]  →  соленоиды
        (вся "умность" здесь)                        (тупой исполнитель)
```

Плата замков не знает ничего про лица/ячейки/пользователей. Умеет только: открыть
замок N, прочитать статус замка N, прочитать статус всех. Вся бизнес-логика — в
терминале. Значит плату можно оставить как есть и менять только хост/софт.

### Терминал (материнская плата)
- OEM-плата, шелкография **ZXW-FACEA-V23, 2025-19**
- Наклейка: **H0-V23C-0003 / Q20250922 / 1G+4G** → 1 ГБ RAM + 4 ГБ eMMC, сборка 22.09.2025
- SoC: **Allwinner** (по разъёмам PFx + площадка FEL). Точная модель — из консоли.
- ОС: **Android** (подтверждено: сервер AndServer)
- Модель на шильдике всего изделия: **KF-10.1AT-Z4WRS5-WIFI**, «人脸识别储物柜控制器»
  (контроллер шкафа с распознаванием лица), дата 09.06.2026
- Версия ПО на экране: **3.2.20250624_12N_F5**

### Экран
- 10.1", **MIPI DSI**, панель BOE
- Код модуля: **MCX101BOE383DZ01**, дата 20251118 (MCX=префикс сборщика, 101=10.1",
  BOE=производитель ячейки). Вероятное семейство матрицы: BOE TV101WXM (1280×800,
  39-pin MIPI, 4 линии, питание 1.8/3.3В). Подсветка БЕЗ драйвера (бустер на матплате).
- Тач по I2C (SCK/SDA), FFC-шлейф панели маркирован HD080-JDD

### Плата замков
- Отдельная **24-канальная** плата (24路锁控板), ревизия 202406_VA
- Питание: вход GND/24V (померить реально! на шлейфе к терминалу подписано 12V — расхождение)
- Связь с терминалом: **UART (TTL, TX2/RX2)**, НЕ RS-485
- Адресация плат: кнопка 地址切换 + светодиоды AD1–AD4 (двоичный адрес, веса 1/2/4/8)
- Каскад: **плата address=1 → ячейки 1–21**, **плата address=2 → ячейки 22–43**
- Входы датчиков двери: 检测1 / 检测2
- Разъём к терминалу (主机口): GND / TXD / RXD / A+ / B- / GND / 24V (даёт и TTL и RS485 + питание)

---

## 2. Разъёмы терминала (по шелкографии)

| Метка | Что | Назначение для проекта |
|---|---|---|
| `GND RX0 TX0 5V` (4-pin) | **отладочная консоль UART** | вход №1: лог загрузки, 115200 8N1 |
| `12V GND TX2 RX2 GND` (6-pin) | линия на плату замков + питание | сниффинг протокола замков; питание терминала |
| `FEL` (площадка) + верхний USB | режим прошивки Allwinner | дамп/заливка eMMC через sunxi-fel |
| RJ45 + USB-A | Ethernet + USB-хост | сеть, флешка, клавиатура |
| `485A 485B GND` | RS-485 | не используется |
| `5V DM0 DP0 GND` | внутренний USB | вероятно модуль камеры |
| FFC-шлейфы | MIPI-дисплей, тач (SCK/SDA), плата камеры | |
| IPEX слева внизу | антенна Wi-Fi | |
| жёлтая "таблетка" + VCC-RTC | батарейка часов | |

**Правило безопасности:** при подключении USB-TTL землю адаптера ОБЯЗАТЕЛЬНО соединять
с землёй платы. На консоль пин 5V НЕ подключать. Шлейфы платы замков не дёргать под
напряжением. В верхнем отсеке БП — 220 В.

---

## 3. СЕТЕВАЯ РАЗВЕДКА (выполнено через Claude Code)

- **IP терминала: `192.168.11.242`** (по Ethernet)
- Открытые порты:
  - **8088** — HTTP, сервер **AndServer/2.1.11** → это веб-панель управления (Android)
  - **14035** — кастомный текстовый командный протокол (отвечает `>>>`, парсит команды).
    Назначение неясно, НЕ трогали.
  - 5555 (adb по сети) — ЗАКРЫТ
  - 22 / 23 / 80 — закрыты (ни ssh, ни telnet)

**Вывод:** adb по сети недоступен. Но веб-API открыт и этого достаточно для управления.

---

## 4. ВЕБ-ПАНЕЛЬ (главная находка)

**URL:** `http://192.168.11.242:8088`
**Пароль администратора: `111111`** (подошёл заводской)
**Фронтенд:** uni-app «DoorCabinet» (Vue/H5)

### Меню DeviceSettings (что видно после входа)
Personnel management (лица) · Records management (журнал) · Clear box setting ·
**Box status** (← сюда для ячеек) · Change Password · Restart · Shut down ·
Door cabinet information · Uploader · Download personnel · Download record ·
Upgrade firmware (← прошивка/APK, опасно)

### КРИТИЧЕСКАЯ ДЫРА В БЕЗОПАСНОСТИ
Пароль проверяют ТОЛЬКО эндпоинты `/Login` и `/sendData`. **Все остальные команды
работают БЕЗ авторизации** — включая открытие ячеек, выключение, установку APK.
Токены не выдаются, фронт просто кладёт пароль в localStorage.
→ Для нашего софта это удобно (не нужен пароль).
→ НО: если такой шкаф стоит в работе — любой в той же сети открывает все ячейки.
  Если это боевое оборудование — закрыть порт 8088 файрволом/VLAN.

---

## 5. КАРТА API (полностью вскрыта)

База: `http://192.168.11.242:8088`

| Путь | Метод | Тело | Назначение | Риск |
|---|---|---|---|---|
| `/Login` | POST | `{password}` | вход; ответ `{code:1}` ок / `{code:0}` ошибка | — |
| `/ChangePassword` | POST | пароли | смена пароля | запись |
| `/GetCode` | GET | — | код устройства (был: `28f2dcc0f6201990`) | чтение |
| `/Lsssued` | POST | `{key}` | активация/лицензия | запись |
| **`/GetBoxList`** | **GET** | — | **список 43 ячеек + статус** | **чтение, безопасно** |
| `/OpenLockTest` | POST | `[address, number]` | открыть по СЫРОМУ адресу платы+замка | ⚠ ОТКРЫВАЕТ |
| **`/OpenBox`** | **POST** | **`[boxnum]`** | **открыть ячейку по лог. номеру 1–43** | ⚠ ОТКРЫВАЕТ |
| `/AllOpenBox` | GET | — | открыть ВСЕ | ⚠⚠ |
| `/SingleClearBox` | POST | box | сбросить статус ячейки | запись |
| `/AllClearBox` | GET | — | сбросить все статусы | запись |
| `/sendData` | POST | `{devicepass, tasktype:"26", data:[n]}` | низкоуровневый UART-passthrough (ЕДИНСТВЕННЫЙ требует пароль) | ⚠ |
| `/GetPerson` | POST | пагинация | список лиц | БИОМЕТРИЯ |
| `/AddPerson` | POST | `{name,...}` | добавить лицо | БИОМЕТРИЯ |
| `/DeletePerson` | POST | `[id]` | удалить лицо | БИОМЕТРИЯ |
| `/uploadPerson` | POST | файл | загрузка фото | БИОМЕТРИЯ |
| `/GetAllRecord` | POST | пагинация | журнал событий | чтение |
| `/RestartShutdown` | POST | — | перезагрузка/выключение | ⚠ |
| `/uploadApk` | POST | файл | установка APK | ⚠⚠ необратимо |
| `/downloadPerson` `/downloadRecord` | GET | — | выгрузка данных | БИОМЕТРИЯ/чтение |

### Структура ячейки (из /GetBoxList)
- 43 ячейки. У каждой `lockAddress: {address, number}`.
- Ячейки 1–21 → `address 1`, `number 1–21`. Ячейки 22–43 → `address 2`, `number 1–22`.
- `state`: 0 = свободна, 1 = занята, 2 = особое (была ячейка 13).
- `type`: 0/1/2 = размеры ячеек.

### Разница /OpenBox vs /OpenLockTest
- `/OpenBox [n]` — по ЛОГИЧЕСКОМУ номеру ячейки (1–43), сервер сам транслирует в плату+замок.
- `/OpenLockTest [address, number]` — по СЫРОМУ адресу платы и номеру замка. Для ячейки 1 = `[1,1]`.

---

## 6. ПРОТОКОЛ ПЛАТЫ ЗАМКОВ (подтверждён из данных API)

Параметры UART: **9600 8N1**, контрольная сумма — **XOR всех предыдущих байт**.

**Открыть замок:**
```
0x8A <адрес_платы> <номер_замка> 0x11 <XOR>
```
Примеры (проверены расчётом):
```
open плата1 замок1:  8A 01 01 11 9B
open плата1 замок5:  8A 01 05 11 9F
open плата1 замок21: 8A 01 15 11 8F
open плата2 замок1:  8A 02 01 11 98
```

**Прочитать статус (байт 0x33 — типовой, НЕ подтверждён для этой платы вживую):**
```
0x80 <адрес> <замок> 0x33 <XOR>        # один замок; замок=0x00 = все
```

Ответ платы на открытие приходит через ~1 с тем же кадром со статусом (0x11 открыт).
Байт открытия 0x11 — ПОДТВЕРЖДЁН (совпал с /OpenLockTest [address, number]).
Байт статуса 0x33 — предположение, проверяется живым status-запросом.

---

## 7. УТИЛИТА lockctl.py (написана в сессии Claude Code)

Python + pyserial. Команды:
```
pip install pyserial
python lockctl.py --port COM3 status-all 1              # безопасно, не открывает
python lockctl.py --port COM3 status 1 5                # статус ячейки
python lockctl.py --port COM3 open 1 5                  # ОТКРЫВАЕТ
python lockctl.py --port COM3 raw "8A 01 05 11 9F"      # сырой фрейм
```
(нужна только при работе с платой замков напрямую, в обход терминала)

---

## 8. ТЕКУЩИЙ СТАТУС ПРОЕКТА (обновлено 2026-08-06)

Сделано:
- [x] Идентифицировано железо (терминал Allwinner/Android, плата замков, экран BOE)
- [x] Найден терминал в сети (192.168.11.242), просканированы порты
- [x] Вскрыта веб-панель (8088), подобран пароль (111111)
- [x] Полностью вскрыт API (19 эндпоинтов), найдена дыра в авторизации
- [x] Подтверждён протокол замков и адресация ячеек
- [x] Написана утилита lockctl.py (прямой UART)
- [x] Прочитан /GetBoxList вживую (без пароля работает)
- [x] **Смысл 1 ПОСТРОЕН и РАБОТАЕТ** — `server.py` (stdlib-прослойка, без зависимостей,
      т.к. pip/PyPI закрыт корп-прокси) + `static/index.html` (сетка 43 ячеек).
      Маршруты: `GET /api/health`, `GET /api/cells` (биометрия вырезана),
      `POST /api/open/{n}`. DRY_RUN по умолчанию ВКЛ. Запуск: `HOST=0.0.0.0 PORT=8090 py server.py`.
- [x] **Развёрнут на порт 8090** (порт 8000 занят другим проектом — km-site uvicorn, НЕ убивать).
      PC LAN IP = **192.168.11.29**. Планшет открывает **http://192.168.11.29:8090**.
      Правило файрвола «Locker Proxy 8090 (tablet only)» пускает TCP 8090 только с 192.168.11.242.
- [x] **Стабилизирован против сброса TCP** AndServer (~30% ресетов) — заголовок User-Agent
      + до 5 ретраев в `_upstream()`. `Connection: close` НЕ добавлять (делает хуже).
- [x] **Фронт панели 8088 полностью вскрыт (JS реверс):** `/sendData` tasktype **26=lock, 27=unlock**,
      тело `{devicepass, tasktype, data:[n]}`. `/uploadApk`: multipart, поле `file`, `formData:{user:"test"}`,
      без авторизации. Новый эндпоинт `/UploadFileToBase64` = загрузка фото (биометрия, не трогать).
      Вендор = **kefaface.com**; облако `http://locker.kefaface.com`, APK `webapk`/`sfapk` на coding.net.
- [x] **Собран киоск-APK (офлайн):** `kiosk-apk/locker-kiosk.apk` — fullscreen WebView
      (`com.poctamat.kiosk`, метка «Локер», min21/target34) на `http://192.168.11.29:8090`.
      Сборка сырым SDK-тулчейном (build-tools 34 + JDK17, без Gradle/интернета) через `build.sh`.
      Подписан, проверен. **НЕ залит/не протестирован на железе.**

Тупики:
- **USB/adb МЁРТВ** — Type-C планшета не энумерируется на PC (charge-only/power-only). Ни adb, ни FEL-девайса.
- **Вендорский webapk не скачать** — coding.net и kefaface.com в Китае (Tencent Cloud), недоступны даже мимо прокси.

Выбранный финал — **Смысл 2.1 (свой UI на РОДНОМ экране планшета через `/uploadApk`)**.
Распознавание лиц НЕ нужно (открытие через UI). Если AndServer(8088) умрёт при уходе штатного
приложения в фон — откат на прямой UART (протокол в разделе 6).

Следующий шаг (ГЕЙТ, до любой заливки):
- [ ] **Сеть восстановления ПЕРЕД `/uploadApk`** — поднять FEL и снять образ eMMC.
      `/uploadApk` необратим, adb/USB мёртв, другого отката нет. FEL насильно делает верхний USB
      Allwinner-девайсом (VID 1f3a), даже если обычный Type-C молчал. Нужно фото платы терминала
      (найти площадку FEL + USB; 220 В в верхнем отсеке БП). sunxi-tools пока не установлен.
- [ ] После бэкапа: залить `locker-kiosk.apk`, проверить (UI на экране + открываются ли замки).

---

## 9. ПЛАН «СВОЙ СОФТ» — три уровня

### Смысл 1: свой софт РЯДОМ (управляет по сети) — РЕКОМЕНДУЕТСЯ начать отсюда
Твоё приложение крутится на отдельной железке (ноут / Raspberry Pi / сервер) в той же
сети и дёргает открытый API терминала. Терминал остаётся как есть.
- Обратимо, риск ноль, строится за дни.
- Свой интерфейс можно показать на экране терминала через браузер в киоск-режиме.

**Архитектура прослойки:**
```
[твой веб-интерфейс] → [твой сервер Flask/FastAPI] → HTTP → [терминал:8088] → замки
```
Сервер: GET /api/cells (обёртка над /GetBoxList) + POST /api/open/<n> (обёртка над /OpenBox).
UI: сетка 43 ячеек, статус цветом, клик = открыть.

### Смысл 2: свой софт ВНУТРЬ терминала
- **2.1** — залить своё APK через открытый `/uploadApk` (можно сделать лаунчером). Не нужен паяльник.
- **2.2** — adb по USB (порт 5555 по сети закрыт, но USB может работать) → install/pull/jadx.
- **2.3** — полная перепрошивка через FEL (крайний случай; берёшь на себя экран/тач/камеру/распознавание).

### Смысл 3 (если совсем): выкинуть терминал
Плата замков → USB-TTL → Raspberry Pi/PC, драйвить lockctl.py, свой экран (HDMI/USB-тач
проще чем родной MIPI). Всё с нуля, включая распознавание (InsightFace/ArcFace на лёгких моделях).

**Маршрут:** сначала выжать максимум обратимым Смыслом 1. Необратимое (перепрошивка) — последнее.

---

## 10. ПРАВИЛА БЕЗОПАСНОСТИ (для любого ассистента)

- Ничего необратимого без явного подтверждения человека (перепрошивка, запись в eMMC,
  удаление, открытие реальных ячеек).
- Перед любой записью в память терминала — сначала снять полный бэкап eMMC.
- НЕ дёргать: `/AllOpenBox`, `/AllClearBox`, `/uploadApk`, `/RestartShutdown`, порт 14035 —
  без явной команды.
- НЕ трогать эндпоинты с биометрией (`/GetPerson`, `/downloadPerson`, `/uploadPerson`,
  `/AddPerson`, `/DeletePerson`) — лица людей не выгружать и не логировать.
- Открытие ячеек — только по явному "да" и только на заведомо ПУСТОЙ ячейке.
- Соленоидный замок: команды "закрыть" НЕТ. Дверь открывается импульсом и закрывается
  рукой (защёлкивается). Это норма.
- В своём софте держать предохранитель DRY_RUN (по умолчанию открытие не шлётся, только логируется).
- Change Password / Shut down по сети — не жать (потеря доступа до физического контакта).

---

## 11. Быстрый старт в новом чате

Скопируй ассистенту: «Вот бриф моего проекта по локеру (приложен). Устройство моё,
чужих данных нет. Продолжаем с раздела 8 (следующие шаги). Хочу [например: построить
свой софт по Смыслу 1 — сервер-прослойку и UI]. Соблюдай правила из раздела 10.»
